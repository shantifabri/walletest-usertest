<h2>Categories</h2>
<hr>
<table class="table">
  <thead class="thead-light">
    <tr>
      <th>Category name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New category', new_category_path, type: 'button', class: 'btn btn-primary' %>
